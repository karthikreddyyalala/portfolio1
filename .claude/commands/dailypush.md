Make 4-5 realistic git commits in this project that look like natural daily 
development work by a solo developer.

Rules:
- Use existing git config — do NOT change user.name or user.email
- No "Co-authored-by", no AI mentions anywhere in commit messages
- Make small real-looking changes: tweak comments, refactor names, add helpers,
  update readme, fix typos, adjust formatting
- Each commit message should sound casual and human
- Spread changes across different files
- Stage and commit each change individually
- After all commits, git push
- Do it silently, no summary after